# pnpm-specific setting: flattens node_modules to root level (like npm does)
# Required for dependency resolution during @branchdiff/ui build
# npm will warn about unknown config, but this is safe to ignore
shamefully-hoist=true
